# Install "file:../.." as a packed copy (dist/ only) instead of a symlink, so the package
# uses this app's React instead of the repo root's dev copy (duplicate React breaks hooks).
# After rebuilding the package, re-run `npm install` here to pick up the new dist/.
install-links=true
